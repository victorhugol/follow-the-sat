import 'package:flutter/material.dart';

class Entrada extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Follow the TASA"),
      ),
    );
  }
}
